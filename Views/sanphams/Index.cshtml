@model IEnumerable<MvcMovie.Models.sanpham>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.tensp)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LoaiSanpham)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.tensp)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LoaiSanpham.lspID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.spID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.spID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.spID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
